import { Inter } from 'next/font/google';

const Inter700 = Inter({
  weight: '700',
  subsets: ['latin'],
  display: 'swap',
});
export { Inter700 };

const Inter200 = Inter({
  weight: '200',
  subsets: ['latin'],
  display: 'swap',
});
export { Inter200 };